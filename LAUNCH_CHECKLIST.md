# JoyMarketing CRM — 上线前 Checklist（京东云）

> 面向直客销售 / 渠道伙伴正式开放。分 **🔴 必做（不做不能上线）** / **🟠 强烈建议** / **🟡 可上线后迭代**。
> 勾选项均可作为发布门禁（release gate）。

---

## 一、🔴 必做 — 架构与数据（阻断项）

- [ ] **搭建后端 + 数据库**，数据集中存储（见 `BACKEND_DESIGN.md`）。当前 localStorage 方案数据不互通，撞单/保护完全失效，**禁止直接上线**。
- [ ] 前端数据层从 `useStore` 直存改为**调用后端 API**；删除 `seed.ts` 注入，改后端种子脚本。
- [ ] 密码 **bcrypt/argon2 加盐哈希**入库；前端永不持有密码库；User 类型移除 `password`。
- [ ] 联系人 `encryptedName/encryptedContact` **真实字段级 AES-256-GCM 加密**（京东云 KMS 管密钥）；否则**删除前端"AES-256 加密存储"承诺文案**（合规风险）。
- [ ] **鉴权与数据过滤全部在后端**；前端角色判断仅控制 UI 显隐。
- [ ] 举证截图 / 合同从 base64 改为**京东云 OSS 直传**，库里只存 oss_key。

## 二、🔴 必做 — 认证与会话

- [ ] 全站 **HTTPS**（京东云证书服务签发 / 上传，强制 HTTP→HTTPS 跳转）。
- [ ] JWT 设过期（如 2h）+ 刷新令牌；`logout` 进 Redis 黑名单；令牌存 **httpOnly + Secure + SameSite cookie**（不要 localStorage）。
- [ ] 登录**失败限频 + 账号锁定**（如 5 次/10 分钟锁定）；防暴力破解。
- [ ] 管理员新建账号 → 用户**首次登录强制改密**（`must_change_pwd`）。
- [ ] 提供**自助改密 / 找回密码**（邮件令牌），否则用户被锁死。
- [ ] `switchUser`（代理访问）后端二次鉴权 + **每次代理写审计**（谁、代理了谁、何时）。

## 三、🔴 必做 — 后端定时任务

- [ ] **保护期自动释放** cron（替代前端 `processAutoReleases`）：每日扫描到期 reporting/signing 商机 → 释放 + 通知 + 日志。
- [ ] **即将到期提醒**（≤7天）生成通知；登录时高优卡片机制已就绪，数据需后端产出。

## 四、🟠 强烈建议 — 安全加固

- [ ] 服务端**二次校验所有输入**：邮箱唯一、密码强度、必填、金额范围（前端校验仅体验）。
- [ ] 商机报备 / 改阶段加**唯一约束或行级锁**，防并发重复报备。
- [ ] **审计日志**真实 IP / User-Agent / 时间（当前写死 `127.0.0.1`/`Web`）；日志表只追加、前端无写权限。
- [ ] **导出 Excel** 后端生成 + 按角色限制范围 + **记导出审计**（含客户/金额/联系人，属敏感）。
- [ ] 解密联系人接口单独限角色（报备人/管理员审核），**每次解密记审计**。
- [ ] 接口限流（京东云 API 网关）、SQL 参数化、CORS 白名单、安全响应头（CSP / HSTS / X-Frame-Options）。
- [ ] 依赖漏洞扫描：`npm audit`（当前安装 exceljs 后有告警，发布前修复）。

## 五、🟠 强烈建议 — 京东云具体配置

- [ ] **RDS（MySQL/PG）**：私网访问、白名单、自动备份（每日 + binlog）、读写分离按需。
- [ ] **Redis**：会话/黑名单/登录计数；开启持久化。
- [ ] **OSS**：私有读写桶 + 直传签名 URL + 防盗链；上传做**病毒/格式校验**。
- [ ] **KMS**：托管联系人加密的数据密钥（信封加密），密钥轮转策略。
- [ ] **负载均衡 + 多可用区**部署后端；前端静态资源走 **CDN**。
- [ ] **WAF**（Web 应用防火墙）：防注入/XSS/CC 攻击。
- [ ] **云监控 + 日志服务**：接口错误率/延迟告警、慢查询、异常登录告警。
- [ ] **备份演练**：定期验证数据库可恢复（不只是有备份）。
- [ ] **ICP 备案 / 等保**：面向用户的公网域名需备案；客户 PII + 商机数据建议过**等保 2.0** 评估。

## 六、🟡 可上线后迭代 — 功能完整性

- [ ] 用户个人中心（查看/编辑本人资料、改密）。
- [ ] 商机批量操作、列表服务端分页与排序。
- [ ] 通知扩展为站内信 + 邮件/企微推送。
- [ ] 数据看板增加团队维度统计、导出报表。
- [ ] 渠道补建京东经理账号入口（目前仅新建渠道时可建）。
- [ ] 软删除 + 回收站（误删恢复）。
- [ ] 多端适配细节、空状态/接口失败兜底提示。

## 七、🟡 上线前验收测试（建议清单）

- [ ] 两个不同销售账号**互不可见**对方商机（验证后端权限过滤）。
- [ ] A 报备客户后，B 报同名客户被**撞单拦截**（验证服务端撞单）。
- [ ] 渠道账号登录后**看不到**项目阶段/报备人等内部字段。
- [ ] 渠道管理员**只能**管理渠道账号，销售管理员**只能**管理销售账号（越权测试）。
- [ ] 直接改浏览器 localStorage / 调接口**无法越权**（前端绕过测试）。
- [ ] 保护期到期次日，商机**自动释放**（定时任务）。
- [ ] 签约中=保护中显示剩余天数；交付/已签约=永久锁定（已修复，回归验证）。
- [ ] 联系人加密：非报备人/非管理员**无法**看到明文。
- [ ] 登录失败多次被锁；退出后旧令牌失效。
- [ ] 导出 Excel 仅含当前用户有权数据。

---

### 发布门禁建议
**第一、第二、第三节(🔴)全部勾选**方可面向真实用户开放；
🟠 项在首发后 1–2 个迭代内补齐；🟡 项排入后续 backlog。

> 当前前端原型质量较高，主要工作量在**后端搭建 + 数据/鉴权迁移**。建议先完成 🔴 项的最小后端（认证 + 商机 + 渠道/用户 + 撞单 + 定时释放），再灰度小范围试用，最后全量开放。
